from keras.models import Sequential
from keras.layers import Dense, LSTM
import numpy as np
import pandas as pd

# Generate some random time series data with 3 features and 1 output feature
print(np.linspace(0,1000,1001))
